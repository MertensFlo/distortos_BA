.install_dependencies:
  script:
  - echo ---------------------------------------------------------------------------------------
  - echo Distortos_BA
  - bash ./.gitlab/scripts/install_dependencies.sh $FLAG_GIT $FLAG_WGET $FLAG_XZ_UTILS $FLAG_ARM $FLAG_BUILD_ESSENTIAL $FLAG_NINJA $FLAG_PHYTON