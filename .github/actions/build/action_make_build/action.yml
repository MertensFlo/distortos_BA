inputs:
  flag_ninja:
    type: boolean

runs:
  using: composite
  steps:
  - name: call action
    uses: MertensFlo/StateOS_BA/.github/actions/build/action_make_build@Testing_workflows
    with:
      flag_ninja: ${{inputs.flag_ninja}}
      flag_cmake_version: false
  - name: build all script
    shell: bash
    run: ./scripts/buildAllConfigurations.sh configurations distortosTest