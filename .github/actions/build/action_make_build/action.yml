inputs:
  flag_ninja:
    type: boolean

runs:
  using: composite
  steps:
  # step not needed
  - if: false 
    name: call action
    uses: MertensFlo/StateOS_BA/.github/actions/build/action_make_build@Testing_workflows
    with:
      flag_ninja: ${{inputs.flag_ninja}}
      flag_cmake_build: false
    
  - name: build all script
    shell: bash
    run: |
      echo build all configs
      ./scripts/buildAllConfigurations.sh configurations distortosTest