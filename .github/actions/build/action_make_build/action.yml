inputs:
  flag_ninja:
    type: boolean

runs:
  using: composite
  steps:
  - name: call action
    uses: MertensFlo/StateOS_BA/.github/actions/universal/action_check_installs@Testing_workflows
    with:
      flag_ninja: ${{inputs.flag_ninja}}
      cmake_version: false
  - name: build all script
    run: ./scripts/buildAllConfigurations.sh configurations distortosTest